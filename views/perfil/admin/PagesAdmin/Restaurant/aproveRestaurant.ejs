
<!DOCTYPE html>
<html lang="pt-PT">
    <head>
        <meta charset="UTF-8">
        <title>Lista de Restaurantes a Aprovar</title>
    
        <!-- Logo do site -->
        <link rel="icon" type="image/png" href="/images/Plataform/LogoSite/logo.png" />
    
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
        <link rel="stylesheet" href="/stylesheets/Perfils/navBar.css">
        <link rel="stylesheet" href="/stylesheets/Perfils/lists.css">
    </head>

    <body>
        <%- include('../../navBar') %>
    
        <div class="container">
            <h1 class="text-center my-4">Lista de restaurantes</h1>
    
            <% if (restaurants.length > 0) { %>
                <div class="row justify-content-center">
                <% for(let i = 0; i < restaurants.length; i++) { %>
                    <div class="col-md-4 mb-4">
                    <div class="restaurant-card text-center border rounded p-3 shadow-sm">
                        <img src="<%=restaurants[i].perfil.perfilPhoto%>" alt="Logo do restaurante" class="img-fluid mb-3 rounded">
                        <h5 class="mb-3"><%= restaurants[i].name %></h5>

                        <div class="btn-group d-flex justify-content-center">
                            <!-- Troca o estado para true-->
                            <form action="/perfil/admin/listRestaurants/aproves/<%= restaurants[i]._id %>" method="POST">
                                <button type="submit" class="btn btn-primary">Aprovar</button>
                            </form>
                            
                            <!-- Apaga o restaurante da BD, é um delete-->
                            <form action="/perfil/admin/listRestaurants/aproves/reject/<%= restaurants[i]._id %>" method="POST">
                                <button type="submit" class="btn btn-danger">Rejeitar</button>
                            </form>
                        </div>

                        <p class="mt-3">
                            <a href="/perfil/admin/listRestaurants/aproves/showRestaurant/<%= restaurants[i]._id %>" class="text-decoration-none">Mais Info.</a>
                        </p>
                    </div>
                    </div>
                <% } %>
                </div>
            <% } else { %>
                <p class="text-center">Sem restaurantes para aprovar</p>
            <% } %>
        </div>
        <%- include('../../../../loading.ejs',{message: " "}) %>


        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
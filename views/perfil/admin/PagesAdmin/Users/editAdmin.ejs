<!DOCTYPE html>
<html lang="pt-PT">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/png" href="/images/Plataform/LogoSite/logo.png" />
    <title>Sign up</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/Perfils/photoPerfil.css">

    <!-- CSS -->
    <link rel="stylesheet" href="/stylesheets/Perfils/Admin/PagesAdmin/Users/editAdmin.css">
  </head>
  <body>
    <main>
      <div class="signup-container">
          <h1>Editar conta</h1>

          <form action="/perfil/admin/updateAdmin/<%= userD._id %>" method="POST" enctype="multipart/form-data" id="signupForm" class="needs-validation" novalidate>
            <div class="mb-3 text-center d-flex flex-column align-items-center">
              <label for="description" class="form-label">Foto de perfil</label>
              <img 
                src="<%= userD.perfil.perfilPhoto %>"       
                alt="Foto de Perfil de <%= user.firstName %>"
                class="profile-pic"
              >
            </div>

            <div class="form-group">
              <label for="perfilPhoto">Foto do Perfil:</label>
              <input type="file" 
                accept="image/*" 
                name="perfilPhoto" 
                id="perfilPhoto" 
                class="form-control">
            </div>

            <%- include('../../../../login/editUserCampos', {userD: userD})%>

            


            <!-- Quando clicar em Dono, tem de aparecer o campo para associar a um restaurante, tambem nmeter isso no add-->
            <button type="submit">Alterar</button>

            <div class="d-grid gap-2">  
                <a href="/perfil/admin" class="btn btn-secondary">Voltar</a>
            </div>
          </form>
      </div>
    </main>

    <!-- Bootstrap JS Bundle (opcional para funcionalidades JS do Bootstrap) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
